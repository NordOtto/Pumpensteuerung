# /opt/pumpe/ota/config.env
# Konfiguration für update.sh — auf dem Pi nach .env kopieren und anpassen.

GITHUB_REPO=NordOtto/Pumpensteuerung
MINISIGN_PUBKEY=/opt/pumpe/ota/minisign.pub

# Optional: für private Repos einen GitHub-PAT ablegen
# (read:packages reicht; in Datei mit chmod 600)
# GITHUB_TOKEN_FILE=/opt/pumpe/ota/.github_token
